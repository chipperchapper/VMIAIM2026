# Agent (Google ADK + Gemini via Vertex AI, authenticated with ADC - no model API key)
google-adk
google-cloud-bigquery
pyyaml
python-dotenv

# Web server
fastapi
uvicorn

# Ingestion
requests

# Tests
pytest
